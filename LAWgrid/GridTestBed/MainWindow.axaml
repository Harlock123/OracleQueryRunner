<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lawgrid="clr-namespace:LAWgrid;assembly=LAWgrid"

        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        Width="800" Height="500"
        x:Class="GridTestBed.MainWindow"
        Title="GridTestBed">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <!-- LAWGrid in top 80% -->
        <Border Grid.Row="0" Grid.Column="0" BorderBrush="Gray" BorderThickness="1">
            <lawgrid:LAWgrid x:Name="TheGridInTest"
                             HorizontalAlignment="Stretch"
                             VerticalAlignment="Stretch"/>
        </Border>
        
        <!-- Button panel in bottom 20% -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- First row of buttons -->
            <Button Grid.Row="0" Grid.Column="0" x:Name="cmdTest1" Content="Database Populate Test Sync" Margin="5"/>
            <Button Grid.Row="0" Grid.Column="1" x:Name="cmdTest2" Content="Database Populate Test ASync" Margin="5"/>
            <Button Grid.Row="0" Grid.Column="2" x:Name="cmdTest3" Content="Test Populate" Margin="5"/>

            <!-- Second row of buttons -->
            <Button Grid.Row="1" Grid.Column="0" x:Name="cmdTest4" Content="Populate With Lists" Margin="5"/>
            <Button Grid.Row="1" Grid.Column="1" x:Name="cmdTest5" Content="Populate From DataTable" Margin="5"/>
            <Button Grid.Row="1" Grid.Column="2" x:Name="cmdTest6" Content="Toggle GreenBar Mode" Margin="5"/>

            <!-- Third row of buttons -->
            <Button Grid.Row="2" Grid.Column="0" x:Name="cmdTest7" Content="Save Grid to Desktop" Margin="5"/>
            <Button Grid.Row="2" Grid.Column="1" x:Name="cmdTest8" Content="Populate With Arrays" Margin="5"/>
            <Button Grid.Row="2" Grid.Column="2" Content="Button 9" Margin="5"/>
        </Grid>
    </Grid>
</Window>
